service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write;
    }
    match /user/{userId}/{allPaths=**} {
      allow write: if request.auth.uid == userId;
    }
  }
}
