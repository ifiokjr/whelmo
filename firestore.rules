service cloud.firestore {
  match /databases/{database}/documents {
    function validUserCreate() {
      return !(request.resource.data.keys().hasAll(["validated"]))
    }

    function validUserUpdate() {
      return request.resource.data.validated == resource.data.validated
        || (validUserCreate() && !(resource.data.keys().hasAll("validated")));
    }
    match /user/{userId} {
      allow read: if request.auth != null;
      allow create: if request.auth.id == userId && validUserCreate();
      allow update: if request.auth.uid == userId && validUserUpdate();
    }
  }
}
